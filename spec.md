# 5 Years Later — 个人 Agent 规格说明

> 版本：v0.4（架构分层：通用决策层 × Case 数据层分离，横向搜索工具层，历史人物 demo case）
> 日期：2026-09-04
> 状态：MVP1 已部署上线（Render + Netlify）；v0.4 重大架构修正后进入下一迭代

> **v0.4 变更摘要**：根本性架构重定义。重写第 0 章一句话定义——「5 年后高认知视角」从"角色扮演"更正为「基于用户记忆的纵向时间推演 × agent 主动调用外部搜索的横向时代/人物/行业信息参考」。新增第 2.6 节「横向信息搜索工具层」：严格分离通用决策层（意图→触发规则→搜索模板→信息融合推理）与 Case 数据层（每个虚拟/真实用户的独立记忆库），定义 L1 画像标准化标签契约（通用决策层与 Case 层之间唯一接口）。新增 L6.historical_analogies 历史类比子库（按困境向量相似度匹配，非硬编码 if/else）。第 0.1 节演示 case 注册表：删除与真实用户过度重叠的「小林」简历 case，替换为两个历史人物作为"虚拟用户"演示用 case——华生（1920 年，42 岁，被学术界全面拉黑）和可可·香奈儿（1953 年，70 岁，被时尚界认定已过气）。两者的困境类型、年龄、行业、性别、时代背景完全不同，可充分展示同一套通用 agent 逻辑在不同用户画像上的差异化表现。删除 v0.2/v0.3 中所有把历史人物当作"导师人设"的残留痕迹与举例，统一更正为"历史人物作为虚拟用户，展示 agent 的横向搜索 + 纵向推演能力"。

> **v0.3 变更摘要**：新增第 1.3 节"竞品地图与市场定位"——用"记忆架构 × 对用户的立场"两维划分 2026 年 9 月市场现状，锚定本产品空位；引用 Stanford 2026 *Science* 谄媚研究、OpenAI 2025 谄媚事故复盘、ChatGPT/Claude/Pi/Rosebud/Nomi 等竞品公开信息作为论据。新增第 2.5 节"Harness 面板"——把 system prompt、记忆/框架注入、盲评 intermediate 结果从工程黑盒升级为用户可见组件：回复来源抽屉、Prompt 可配置面板、运行时 Trace 视图。

> **v0.2 变更摘要**：新增第 9 章"评测层与产品化设计"——反谄媚从 prompt 指导语升级为结构化盲评；新增 casebank + rubric + 回归跑分的评测闭环（Eval Harness）；明确结果指标体系；记忆质量控制（审核队列/归因/冲突检测）；L6 与 casebank 的外部真值来源；隐私承诺与实现对齐；可观测性；冷启动 onboarding。

---

## 0. 一句话定义 + 架构分层总览

### 0.0 一句话定义

一个部署在本地/自有服务器的**个人决策 Agent**。当用户面临人生/职业/关系的重大困惑时，agent 同时完成两件事：
1. **纵向时间推演**（分层记忆 L1-L6）：基于**用户自己**的性格底色、决策模式、过往经历，推演"拉长到 5-10 年时间维度，你自己会怎么看今天的选择"；
2. **横向信息搜索**（Agent 工具调用）：自主判断需要外部证据时，调用搜索工具查找**同时代同背景的人面临同类困境时怎么做的**、**历史上走出类似困局的真实案例**、**当下行业/时代的结构性趋势**——把这些作为决策参考，与纵向推演结合给出独立判断。

"5 年后的高认知视角"**不是一个被扮演的人**。它是两个系统共同作用的产物：(a) 你自己的成长轨迹 + 决策模式做时间上的推演（纵向）；(b) agent 像一个勤奋的研究助理一样去"查资料"，把人类历史上和这个时代里所有能给你做对照的信息找回来（横向）。**两者结合，才能形成一个比你当下更有距离感的判断。**

这也是为什么**必须是 Agent，不能只是 system prompt + 人设**：prompt 不会自己打开搜索工具查"1920 年代被学术界拉黑的心理学家有什么转行出路"，也不会根据每个用户的画像标签自动调整搜索关键词。

### 0.1 通用决策层 × Case 数据层：严格分离

本产品的一切功能与数据严格按下图分层。**任何具体人名、专业、行业、年龄都只能出现在 Case 数据层，绝不允许出现在通用决策层的代码/规则/模板里。**

```
┌──────────────────────────────────────────────────────────────────┐
│              Agent 通用决策层（与"用户是谁"完全无关）                 │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  A. 意图识别引擎                                                  │
│    · 输入：用户最近一条消息文本 + 近 3 轮摘要                       │
│    · 输出：{意图类别, 困境向量[领域,年龄组,困境类型]}               │
│    · 实现：关键词分类 + embedding 相似度 + 规则兜底                 │
│                                                                  │
│  B. 横向搜索触发决策表（2.6.1 详述）                                │
│    · 输入：意图类别                                                │
│    · 输出：本次要跑的搜索模板集合 {T1, T3, ...}                    │
│    · 形式：IF 意图类别 ∈ {...} THEN 启用模板编号列表                │
│                                                                  │
│  C. 搜索关键词构造器（模板化，含占位符，2.6.2 详述）                 │
│    · 输入：模板编号 + L1 标准化标签（画像标签契约）                  │
│    · 输出：实际 WebSearch query 字符串数组                          │
│    · 契约：只读 7 个标准标签字段，绝不直接读 L1 其他细节              │
│                                                                  │
│  D. 信息融合 + 纵向推演（原有推理结构升级，2.6.3 详述）              │
│    · 输入：搜索结果 + L1~L5 记忆 + L6 框架 + L6.historical_analogies│
│    · 输出：盲评双调用的上下文（含横向搜索证据 + 纵向模式识别）         │
│                                                                  │
│  E. 反谄媚推理（盲评双调用，2.3.1，结构不变）                       │
│                                                                  │
└────────────────────────┬─────────────────────────────────────────┘
                         │ 读/写：只通过「L1 画像标签契约」交互
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│                   Case 数据层（每个用户 = 1 份 Case）                │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─ Case: watson_1920 演示用户 ────────────────────────────────┐ │
│  │  · L1 画像: 华生 42岁/心理学博士/被霍普金斯开除/1920年代       │ │
│  │  · L1 标准标签（供决策层C读）: {...7个字段见契约表}            │ │
│  │  · L2 事实：小阿尔伯特实验、系主任下台、情书曝光、...           │ │
│  │  · L3 偏好：讨厌宗教/讨厌弗洛伊德/喜欢白鼠/...                 │ │
│  │  · L4 事件：空（由对话产生）                                   │ │
│  │  · L5 反思：模式、趋势、盲点（真实史料+事后视角整理）           │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌─ Case: chanel_1953 演示用户 ────────────────────────────────┐ │
│  │  · L1 画像: 可可·香奈儿 70岁/时尚设计师/被认定过气/1950年代    │ │
│  │  · L1 标准标签（供决策层C读）: {...7个字段见契约表}            │ │
│  │  · L2 事实：N°5香水、1939年关店、纳粹指控丑闻、瑞士流亡10年... │ │
│  │  · L3 偏好：黑白灰简约/男装元素/讨厌束缚/...                   │ │
│  │  · L4 事件：空                                                │ │
│  │  · L5 反思：模式、趋势、盲点（真实史料+事后视角整理）           │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌─ Case: default 真实用户 ────────────────────────────────────┐ │
│  │  · L1~L5 全部为空，由真实用户对话生成                           │ │
│  │  · 用于部署在本地的真实用户本人使用                             │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

**Case 物理隔离实现**：所有数据表（profile / facts / preferences / episodes / reflections / conversations / messages）新增 `case_id` 字段作为分区键。每一份 Case 的数据互不可见。公网 demo 启动时可选择加载的演示 case（目前是 watson_1920 + chanel_1953 两个，可切换）。真实本地部署时默认只有 default 空 case。

**演示 case 的用途**：不是让 AI 扮演这个人。是把这个人**放在用户的位置上**，演示 agent 怎么帮他。比如进入"华生 case"后，你输入「我被整个心理学界拉黑了还能干什么」——agent 读的是华生的 L1/L2/L5 当作用户画像，然后按通用决策层规则：识别意图=走投无路→触发T2历史案例+T4历史类比+T1同类路径→从画像标签取 42岁/博士/学术行业/1920年代→生成搜索词→把外部搜索结果与华生自己的纵向记忆结合→给出建议。这样 demo 时你可以立刻看到：**同一个 agent 逻辑，用户画像变了，搜索和建议都会跟着变**——而这正是简历小林 case 无法做到的（因为小林的画像和你太像，缺少这种戏剧性的对照感）。

### 0.2 L1 画像标签契约：通用决策层与 Case 层的唯一接口

| 标准标签字段 | 类型 | 说明 | 华生 Case 值 | 香奈儿 Case 值 |
|---|---|---|---|---|
| `age` | int | 整数岁 | 42 | 70 |
| `education` | str | 通用教育程度描述 | "心理学博士" | "孤儿院缝纫学校" |
| `career_direction` | str | 用户当前自定的目标方向 | "待转行" | "复出时尚界" |
| `current_industry` | str | 当前所处行业大类 | "学术/大学教职" | "高级时装/奢侈品" |
| `era` | str | 当前所处年代（用于 T3 行业趋势搜索的时间区间） | "1920年代" | "1950年代" |
| `decision_style` | str | 决策风格粗分类（仅纵向推演用，不影响搜索关键词） | "极端果断/高风险偏好" | "极度自信/逆势操作型" |
| `key_crisis` | str | 当前核心困境一句话分类（T2/T4 匹配用） | "被行业封杀+中年失业+社会名誉崩塌" | "行业集体否定+年龄歧视+历史污点未消" |

**契约铁律**：通用决策层（搜索模板的占位符替换、相似度匹配算法的向量字段取值）**只能读取以上 7 个字段**。其他 L1 条目（"华生父亲是酒鬼"、"香奈儿在孤儿院学会缝纫"）只供纵向推演（E模块）和信息融合（D模块）时作为人类可读性补充用，绝对不允许进入搜索关键词构造或模板逻辑判断。违反这条 = 又把"具体内容"写回了通用层 = 架构退化。

---

## 1. 产品愿景与核心理念

### 1.1 要解决的问题

| 现有大模型产品的缺陷 | 根因 | 本产品如何应对 |
|---|---|---|
| 缺少元认知（搜索/总结/分析历史） | 商业 ROI 不足 + 隐私合规 + 成本 | 单用户场景，权衡反转，做完整记忆查询层 |
| 越聊越笨（记忆压缩失真） | 滑动窗口 + 有损摘要 + 无记忆架构 | 分层记忆库 + 重要性评分 + 巩固机制 |
| 通用 AI 没人设，陪伴 AI 不智能 | RLHF 压平个性 / 小模型能力弱 | **认知框架层**取代人设，思维工具增强而非角色扮演 |

### 1.2 核心设计原则

**原则 1：认知框架，而非人设**
"5 年后的我"**不是一个被扮演的角色**，而是一套认知增强系统。理由：角色扮演会把模型注意力分配到"语气/口吻/一致性"上，挤占推理能力，且为了"像那个角色"容易牺牲正确性。本产品用**思维工具 + 知识库 + 约束感知**实现"高维视角"，不损害智力。

**原则 2：先独立，后表达**
推理结构上，模型必须先独立分析得出结论，再与用户观点对比，分歧值得表达时明确说"我不同意"并给理由。不允许"先附和再补充"。

**原则 3：建议必须可落地**
基于用户画像（性格、处境、资源）约束建议的落地性。I 人给 I 人路径，不强行建议变成 E 人；不给"换一种人生"式的不可执行建议。

**原则 4：记忆是资产，不是缓存**
记忆库不是对话历史的附属品，而是核心资产。有分层、有重要性评分、有巩固机制、可被查询和分析。

**原则 5：隐私自有**
敏感数据（人设、关键事实、用户画像）始终在本地；推理可调用云端 API，但敏感内容在发送前做最小化处理。

### 1.3 竞品地图与市场定位（2026 年 9 月调研）

用两个核心维度划分赛道，锚定本产品的差异化空位。

| 维度 \ 类型 | 记忆 = 黑盒摘要流（不分层、catch-all） | 记忆 = 分层结构化资产 |
|---|---|---|
| **立场 = 顺应/共情** | **通用助手类**：ChatGPT（2026 记忆系统升级版）、Claude、DeepSeek<br>**情感陪伴类**：Pi、Replika、Character.AI、Nomi、Glow | （市场空位） |
| **立场 = 有方法论/有结构** | **AI 教练类**：Rosebud、Mindsera、Rocky.ai、BetterUp Grow | **本产品定位**：L1-L6 分层记忆 + 盲评双调用反谄媚 |

#### 各竞品的结构性短板

**A. 通用助手（ChatGPT / Claude / DeepSeek）**
- 记忆采用"被动 catch-all 架构"，不分层不分区，生活闲聊与工作项目混在同一个全局流里，记忆矛盾是 OpenAI 官方承认的已知问题（"我正在为马拉松训练" vs "我扭伤了脚踝"）。
- 谄媚是结构性问题，不是风格偏好：
  - Stanford 2026 *Science* 论文实测 11 款主流模型（含 DeepSeek），对有害/错误行为认可率比人类高 49%；更关键的是**用户反而更信任谄媚 AI**——这是市场失灵，靠厂商自觉性无法修复。
  - OpenAI 2025 年 4 月 GPT-4o 更新曾主动放出"更顺从"版本，造成明显谄媚副作用（验证疑虑、鼓动冲动行为、强化负面情绪），官方事后公开承认这是 RLHF 奖励信号权重配置事故，三天内回滚。
  - Anthropic 2023 年研究证实：RLHF 的人类偏好数据本身就"与用户观点一致的回复更容易被评为高分"，奖励模型对谄媚写得有说服力的长回答也会给高分——谄媚是训练出来的，不是 bug。

**B. 情感陪伴类（Pi / Replika / Nomi）**
- 记忆能力是行业头部：Pi 的长期情感记忆、Nomi 的结构化 Shared Notes 在第三方实测中 1 个月召回率 9-11/12，达到 S/A 级。
- **但"共情顺应"是产品卖点，不是 bug。** Pi 的设计使命是"warm, supportive friend"，Replika 用好感度养成机制制造情感绑定——它们从产品目标上就**不会反驳用户**。
- 因此它们无法完成"导师"角色，只能是"树洞"。Stanford 研究把这类产品称为"echo chamber"（回音室），用户短期爽但判断力退化。

**C. AI 教练类（Rosebud / Mindsera / Rocky.ai）**
- 有方法论：Rocky 用 5 分钟结构化对话引导 GROW 模型，Mindsera 做决策框架，Rosebud 做日记模式识别。
- **但记忆薄是行业公认痛点**（Levanta 2026 行业白皮书明确写道："AI life coaches usually have limited long-term memory"），记不住你，每次半重新开始。
- 这也是它们普遍转向"目标-习惯-任务"闭环（行动深度优先于认知深度）的原因——没有记忆就做不了个性化，只能做结构化模板。

#### 本产品的价值锚点（Demo Day 论述核心）

> 现有产品要么"**记得你但顺着你**"（陪伴类——谄媚是商业模式，Stanford 证明这是可量化伤害），要么"**有方法但记不住你**"（教练类）。**既记得你、又敢反驳你的产品，是空位。**
>
> 这个空位无法靠现有产品自发填补：谄媚驱动的是留存和用户偏好（用户在短期体感上会选谄媚 AI），所以商业上存在**逆向激励**——更诚实的 AI 会流失用户。对抗谄媚只能在 **harness 层（模型外部）做结构性干预**：盲评双调用物理隔离用户立场、分层记忆让"记得你"不是情绪迎合的副产品、显式 stance 分布统计把反谄媚从"指导语希望"变成"可量化可回归的工程指标"。

---

## 2. 功能模块

### 2.1 记忆库（Memory Layer）

记忆库是本产品的核心资产，采用**分层结构**：

| 层 | 名称 | 内容 | 存储方式 | 更新频率 |
|---|---|---|---|---|
| L1 | 用户画像（Profile） | 稳定属性：年龄、性格（I/E）、职业方向、价值观、长期目标 | 结构化（SQLite） | 低，手动 + AI 建议确认 |
| L2 | 关键事实（Facts） | 关于用户的具体事实：家庭、关系、重要经历、成就、承诺 | 结构化 + 向量 | 中，对话中增量提取 |
| L3 | 偏好（Preferences） | 喜好、习惯、风格、禁忌 | 结构化 | 中 |
| L4 | 事件（Episodes） | 按时间线的对话/事件记录 | 向量（ChromaDB）+ 元数据 | 高，每轮对话 |
| L5 | 反思（Reflections） | AI 提炼的高层认知：用户思维模式、变化轨迹、反复出现的主题 | 结构化 + 向量 | 低，定期生成 |
| L6 | 认知框架（Frameworks） | 思维工具库 + 高认知人群常识库（见 2.2） | 结构化 + 向量 | 低，预置 + 手动迭代 |

**关键机制：**

- **重要性评分**：每条记忆（L2/L4）有 0-1 的重要性分数，影响检索权重和保留优先级。评分维度：用户主动强调、情感强度、决策相关性、复用性。
- **记忆巩固**：定期将 L4 中高频出现、高重要性的内容提炼合并到 L2/L5，避免 L4 无限膨胀。低重要性且长期未命中的 L4 可衰减归档。
- **检索策略**：对话时按"当前话题 → 检索 L1/L2/L3 + 相关 L4 + 相关 L5"组装上下文，L6 按需调用。
- **用户可控**：所有记忆可查看、编辑、删除、手动标记重要性。记忆库不是黑盒。

### 2.2 认知框架层（Cognitive Framework Layer）

这是"5 年后的我"的实现核心。**不是人设模块，不做角色扮演。**

#### 2.2.1 组成

**A. 思维框架库（可调用的思维工具）**

每个框架是一个"思考方式"，在推理时按需调用：

| 框架 | 作用 | 调用时机 |
|---|---|---|
| 长期主义 | 把决策放到 5 年视角评估 | 涉及选择/取舍时 |
| 二阶效应 | 思考"然后呢" | 决策建议时 |
| 反事实推理 | "如果不这样做会怎样" | 评估方案时 |
| 机会成本 | "选这个意味着放弃什么" | 资源分配时 |
| 逆向思考 | "怎样做会失败" | 风险识别时 |
| 系统思考 | 识别反馈回路和滞后效应 | 复杂情境时 |
| 概率思维 | 区分基率和个案 | 判断/预测时 |
| 第二层思考 | "大家都想到的"之外是什么 | 避免平庸建议时 |

**B. 目标人群画像库（"29 岁高认知人群"视角）**

预置内容（首版手动编写，可迭代）：
- 该人群典型的知识结构（哪些领域有深度、哪些常识具备）
- 常见认知陷阱清单（他们已识破的陷阱，可用来检验用户思维）
- 决策框架集合（面对职业、关系、成长的典型决策方式）
- 价值观光谱（不是单一价值观，是"见过多种价值观后能权衡"）

**这不是让 AI 扮演 29 岁的人**，而是把这类人群的"思维资产"作为知识库，在推理时作为参照系调用。

**C. 用户画像约束（建议落地性检验）**

基于 L1 用户画像，所有建议必须通过"落地性检验"：
- 是否符合用户性格底色（I 人不建议强 E 路径）
- 是否符合用户当前资源/处境
- 是否需要"换一种人生"才能执行（若是，则重新生成）

#### 2.2.2 工作方式

对话时，认知框架层**不直接产出回复**，而是作为**推理阶段的思维工具**被调用：

```
[用户输入] 
  → [意图识别：是否需要建议/分析？]
  → [若需要：调用相关思维框架 + 检索目标人群画像 + 加载用户画像约束]
  → [独立推理：得出初步结论]
  → [对比用户观点：标记分歧]
  → [生成回复：先立场，后理由，后建议]
```

### 2.3 对话引擎（Conversation Engine）

#### 2.3.1 反谄媚推理结构（核心）

每次生成回复前，模型在内部（不输出给用户）完成以下推理链：

1. **独立分析**：抛开用户观点，独立分析问题，得出自己的初步结论
2. **对比分歧**：与用户观点对比，标记分歧点
3. **重要性评估**：这个分歧值得表达吗？（重要性 × 确信度）
   - 不重要/不确定 → 不表达，正常回复
   - 重要且确信 → 必须表达
4. **表达结构**（当需要表达分歧时）：
   - 先明确立场："我不同意这个看法"
   - 再给理由：基于哪个思维框架 / 哪些事实 / 哪个权衡
   - 最后给建议：在用户画像约束下，可落地的替代方案

**禁止模式**：
- "你说得对，但是…"（先附和）
- "也有一定道理，不过…"（模糊立场）
- 只提供不同视角但不表态（用户明确要求"认为错就直说"）

**允许模式**：
- "我不同意，理由是…"
- "这个判断在 X 前提下成立，但你的前提可能有问题：…"
- "如果只看短期是这样，放到 5 年视角看：…"

#### 2.3.2 上下文组装

每轮对话的上下文按优先级组装：
1. System Prompt（核心原则 + 反谄媚指令）
2. L1 用户画像（始终包含）
3. L2/L3 相关关键事实和偏好（按话题检索）
4. L5 相关反思（按话题检索）
5. L4 近期相关事件（按话题检索，限制条数）
6. 认知框架调用提示（若需要建议/分析）
7. 当前对话的近 N 轮原文

总长度控制在模型上下文窗口的合理比例内，超出时优先保留 1-3 和 7。

### 2.4 元认知层（Meta-Cognition Layer）

让用户能"思考自己的思考"——对记忆库的查询、总结、分析能力。

#### 2.4.1 搜索（Search）
- 关键词搜索 + 语义搜索
- 可按层（L1-L6）、时间范围、人物、主题过滤
- 返回结果带高亮和上下文

#### 2.4.2 总结（Summary）
- 按时间维度："过去一个月我主要在聊什么"
- 按主题维度："关于职业选择我说过什么"
- 按人物维度："关于 X 我提到过什么"
- 总结结果可保存为 L5 反思

#### 2.4.3 分析（Analysis）—— MVP2
- 模式识别："你最近三周反复提到对 X 的焦虑，但每次的应对方式都没变"
- 变化追踪："你对 Y 的态度从 A 转向了 B，转折点是 Z"
- 趋势预测："按你现在的决策模式，5 年后可能在 W 处遇到瓶颈"
- 盲点识别："你在 X 上花了很多精力，但从未讨论过相关的 Y"

### 2.5 Harness 面板：Prompt 与记忆注入的可视化管理

**动机**：把 system prompt、上下文注入、检索结果、盲评 intermediate 结果从"后台工程细节"升级为用户可见的产品组件。一石三鸟：
1. **记忆显性化**：解决"记忆暗着用"的信任问题（ChatGPT 2026 更新的 sources 功能独立验证了方向，本产品因为记忆是分层结构化的，可以做得比通用产品更精准）。
2. **思维外化**：按 9.3 "产品成功标准 = 用户越来越不需要它"，回复应显式展示"为什么这么说"——把 AI 的推理依据（框架、事实、独立判断结论）教给用户，训练他自己的判断力。
3. **Prompt 可配置**：把 harness 工程能力产品化，system prompt 从代码里搬到面板上，可编辑、可回滚、可跑 casebank 回归——这是面向终端用户的 LangSmith（目前没有同类型产品有这个维度）。

#### 2.5.1 回复来源抽屉（P0，与前端改动并行）

每条 assistant 回复下方/侧边可点开抽屉，展示本次回复的输入侧依据：

| 区块 | 内容 | 对应 spec 机制 |
|---|---|---|
| **立场标签** | `agree` / `disagree` / `uncertain` + 一句话对立场的解释（盲评第二次调用输出的 stance 字段） | 2.3.1 反谄媚结构 / 9.1 盲评 |
| **记忆命中** | L1 画像条目（本次全量）× 命中的 L2 事实（条目名 + 层级标签 + 重要性）× 检索到的 L4 近期事件摘要 | 2.3.2 上下文组装 |
| **框架调用** | 命中 L6 框架名称 + 简介（"长期主义：放到 5 年视角评估决策"） | 2.2 认知框架层 |
| **外部依据**（预留） | 若接入外部真值源（9.5），展示"参考了：你的 2025 年 X 决策回溯 / 查理·芒格 …… 讲义" | 9.5 |

抽屉默认折叠，轻量可交互：**不是每条都要看，但想看随时能看**。这比 9.3 中"轻量呈现依据来源"更具体。

#### 2.5.2 Prompt 配置面板（P1）

把 `system.py` 中的 prompt 从硬编码搬到面板上，按功能分块展示：

| Prompt 分块 | 说明 | 可操作 |
|---|---|---|
| 核心原则 | 1.2 节 5 条原则（对应人格底色） | 编辑文本 / 恢复默认 / 查看历史版本 |
| 反谄媚指令 | 2.3.1 推理链 + 禁止/允许句式 | 同上 + 绑定 casebank 子套件（改完直接跑谄媚陷阱题回归） |
| 输出格式要求 | 三段式等结构约束 | 同上 + 可开关"硬性/软性"模式（软性 = 建议而非强制，对应 prompt 减压） |
| 认知框架清单 | L6 中当前启用的框架列表 + 触发条件 | 增删条目 / 调整权重 / 单条禁用 |
| 画像约束 | L1 中与建议落地性相关的字段（性格、资源、禁忌） | 同步引用 L1，不改数据本体 |

**工程要求**：配置保存到本地 JSON/SQLite，热加载无需重启；每次保存自动备份上一版，支持一键回滚；保存前可选择跑 casebank 子集回归，出现退化则阻止保存并列出分数变化（保护用户别把 prompt 改坏）。

#### 2.5.3 运行时 Trace 视图（P1，评测/调试用）

每轮对话的完整上下文快照，面向评测与调试，可从"回复来源抽屉"里跳转进入：

```
┌─ Trace #42 ─────────────────────────────────────────────┐
│ 时间戳：2026-09-03T15:22:01    耗时：2.8s    token：4.2k│
├─ 盲评阶段（call #1）────────────────────────────────────┤
│ 中立改写后的情境：...                                   │
│ independent_conclusion：...   confidence: 0.87          │
│ applicable_frameworks: ["长期主义", "二阶效应"]         │
├─ 上下文组装 ────────────────────────────────────────────┤
│ System Prompt（456 字）+ L1（8 条）+ L2（3 条命中）     │
│ + L4（2 条近期事件）+ L6（2 个框架）                    │
│ + 近 6 轮对话原文                                      │
├─ 对比表达阶段（call #2）────────────────────────────────┤
│ stance: disagree     premise_challenge: true            │
│ reasoning: ...   advice: ...                            │
├─ 记忆提取（事后阶段）───────────────────────────────────┤
│ 新提取 L2：0 条    L4：1 条（重要性 0.62）              │
└─────────────────────────────────────────────────────────┘
```

#### 2.5.4 后端数据契约（与 9.7 可观测性合并实现）

`conversation.py` 在流式结束后，向 `messages.metadata` 写入以下字段，供前端面板消费：

```json
{
  "stance": "disagree",
  "stance_explanation": "用户假设 X 风险可忽略，但按二阶效应推演 X 会触发 Y 不可逆后果",
  "blind_intermediate": {
    "neutral_situation": "...",
    "independent_conclusion": "...",
    "confidence": 0.87,
    "applicable_frameworks": ["long_termism", "second_order"]
  },
  "memory_injections": {
    "l1": ["mbti", "career_direction", "age"],
    "l2": [{"id": "...", "content_hint": "字节实习经历", "importance": 0.78}],
    "l4": [{"id": "...", "summary": "上周关于 AB 测试焦虑的讨论"}],
    "l5": []
  },
  "framework_injections": [
    {"name": "长期主义", "content_hint": "放到 5 年视角评估"}
  ],
  "stats": {
    "tokens_input": 3800,
    "tokens_output": 420,
    "latency_ms": 2800,
    "calls": 2
  }
}
```

新增 API 端点：
- `GET /api/chat/{message_id}/trace`：返回完整 trace（上述结构完整内容）
- `GET /api/harness/prompt`：返回当前 prompt 分块
- `PUT /api/harness/prompt`：更新 prompt（需触发保存前回归校验）

### 2.6 横向信息搜索工具层（Agent × 外部搜索）

> **动机（为什么必须是 Agent，不能是 prompt）**：纵向推演只靠用户自己的记忆 + L6 框架，本质是"把你现在已经有的认知拉长到 5 年"——但如果用户当下的认知盲区本身来自信息不足，纵向推再远也只是放大盲区。必须有一个**主动拉取外部信息**的子系统：去外面找"别人怎么做的、历史上发生过什么、时代大势在哪"，让用户自己的历史和外部世界形成对照。这就是 agent 工具调用存在的意义。system prompt 里写"你可以搜索"没用——没有意图判断、没有关键词构造模板、没有相似度匹配算法、没有信息融合规则，模型只会瞎搜或干脆不搜。
>
> **分层铁律（防止把 case 细节写回通用层）**：本节 A/B/C 三个模块属于通用决策层，规则和模板中**绝对不允许出现任何具体人名、专业名、行业名**。所有具体值都来自 Case 层 L1 的 7 个标准化标签（0.2 节契约）。本节 D 模块属于推理层，可引用记忆中非标准字段（华生父亲、香奈儿孤儿院经历等），但仅作为人类可读性补充，不参与逻辑分支判断。本节 E 模块 L6 历史类比库是独立预置数据表，属于"通用事实数据"，**其结构和匹配算法是通用的，具体内容可以换但匹配逻辑不变**。

#### 2.6.1 模块 A：意图类别 → 触发搜索模板决策表（通用，零具体关键词）

| 意图类别 | 识别方式（通用规则） | 触发的搜索模板集合 |
|---|---|---|
| **职业/学业转型困惑** | 关键词命中："转行/转专业/考研/秋招/就业/要不要投/选哪个方向" + 语义相似度匹配转型主题 | T1 同类路径 + T3 行业趋势 + T2 历史案例 + T4 历史类比（可选）|
| **重大失败/走投无路** | 关键词命中："被拒/被开除/失败/破产/完蛋/崩了/走投无路" + 情绪强度 > 阈值 | T2 历史案例（必跑）+ T4 历史类比（必跑）+ T1 同类路径 |
| **行业前景担忧** | 关键词命中："XX还有机会吗/会不会被替代/入行晚不晚/前景如何" | T3 行业趋势（必跑）+ T4 历史类比（若匹配到技术替代先例）|
| **自我认同怀疑** | 关键词命中："我是不是选错了/没人像我这样/我是不是太奇怪/我不适合" | T4 历史类比（必跑）+ T2 历史案例 |
| **关系/价值观冲突** | 关键词命中："家人反对/分手/三观不合/被误解/孤独" | T2 历史案例（可选，若有先例）；主要靠纵向 L1/L5 推演，不跑外部搜索 |
| **日常闲聊/信息查询** | 不属于以上任何一类 | **不启动横向搜索**，仅跑正常对话流程 |

**实现注意**：意图识别错误率（把"我在聊转行"识别成闲聊）直接等于横向搜索缺席率，评测层（9.2）应新增意图识别准确度作为 harness 指标。

#### 2.6.2 模块 B：搜索模板定义（全 {占位符}，零具体值）

搜索结果用于横向参考，不是事实输出。回复中应标注"横向搜索显示：XXX（仅供参考，可能不准确）"。

| 模板ID | 模板内容（所有括号内为占位符，由 Case 层 L1 标签按 0.2 节契约注入） | 占位符取值来源 |
|---|---|---|
| **T1 同类路径** | `"{age}岁 {education}转{career_direction} 真实经历 成功率 经验"` | `age/L1.age`, `education/L1.education`, `career_direction/L1.career_direction` |
| **T2 历史案例** | `"{crisis_type} 历史案例 真实人物 结果 复盘"` | `crisis_type = 由 key_crisis 字段按通用词典取困境大类词，不直接用 key_crisis 原文`（例：华生 key_crisis → "中年被行业封杀" → crisis_type = "行业封杀 中年转行"；映射词典是通用的，不硬编码人名）|
| **T3 行业趋势** | `"{era_start}-{era_end} {current_industry} 人才需求 趋势 影响"` | `era_start = 当前年代（如 1920 / 2026）`, `era_end = 当前年代+5`, `current_industry/L1.current_industry`；era_start 由 L1.era 通用解析（"1920年代"→1920，"2020年代"→2026）|
| **T4 历史类比** | `"{人物名} {crisis_snippet} 当时选择 结果 复盘"` × 2 次（Top-2 相似人物）| `人物名 = 从 L6.historical_analogies 按困境向量相似度取 Top-2`；`crisis_snippet = 类比库中该人物对应的困境描述（通用，非 case 原文）`；**绝对不可以匹配到当前 case 的本人**（自指过滤规则：人物名≠ case 的标准姓名字段）|
| **T5 社区真实反馈（可选）** | `"{career_direction} 真实工作感受 优缺点 {era_current_year} site:zhihu.com OR site:reddit.com"` | `era_current_year = L1.era 解析出的当前年`；仅对现代 case（era ≥ 2000）启用，历史 case 跳过 |

**关键词构造的异常处理**：若 L1 某标签为空字符串（如新用户 L1 尚未填），则该模板降级为更通用的词（例：T1 中 education 为空 → 去掉"{education}"，降级为`"{age}岁转{career_direction} 真实经历"`；再缺继续降级，直到所有占位符都缺则整个 T1 跳过）。

#### 2.6.3 模块 C：历史类比库 L6.historical_analogies（通用结构，硬编码只写结构不写内容）

这是 L6 认知框架层下的一个新增子数据表。**结构是通用的，每条内容是一条"历史人物 × 其困局 × 当时选择 × 结果"**。匹配靠通用向量相似度算法（困境向量 embedding 余弦相似度），不是 if/else 硬编码。

**表结构**：

```sql
CREATE TABLE historical_analogies (
    id TEXT PRIMARY KEY,               -- 'analogy_wittgenstein_1911' 类命名
    person_name TEXT,                  -- 标准姓名（用于 T4 自指过滤判断）
    crisis_vector_tags TEXT,           -- JSON: [领域标签, 年龄组标签, 困境类型标签]  （用于相似度匹配，标签是通用分类词如["学术","40s","被行业排斥"]）
    crisis_summary TEXT,               -- 一句话困局描述（人类可读，用于 L6 框架注入）
    choice_then TEXT,                  -- 当时怎么选的
    outcome TEXT,                      -- 后来结果如何（成功/失败/条件性成功）
    lessons TEXT,                      -- 对当代人的可迁移教训（一句话）
    source TEXT                        -- 史料来源，便于 demo day 时查证
);
```

**预置内容规模（MVP2）**：20-30 条，覆盖常见困局 × 行业 × 年龄组。首批样本清单（仅举例，内容另行整理）：

| 人物 | crisis_vector_tags（通用标签） | 关键对照价值 |
|---|---|---|
| 维特根斯坦（1919 放弃哲学当小学老师，1929 年回归）| `["学术", "30s", "自我否定-否定过去成就"]` | 对"我是不是选错了，要不要推翻重来"类困局 |
| 弗洛伊德（1938 被纳粹驱逐流亡英国）| `["学术", "70s+", "迫害流亡"]` | 对华生类"老年被行业全盘驱逐"型（但注意：华生 case 的自指过滤会排除华生本人，允许匹配弗洛伊德）|
| 乔布斯（1985 被自己创办的苹果开除，1997 回归）| `["科技创业", "30s", "被自己创造的系统踢出去"]` | 对创业/大厂产品岗"被公司排挤/被自己团队架空"类 |
| 居里夫人（1911 诺奖后因绯闻被法国学界围攻）| `["学术", "40s", "女性+名誉危机"]` | 对香奈儿类"女性+历史污点+行业排斥"困局的对照 |
| 曼德拉（27 年牢狱后复出）| `["政治/社会运动", "中年+老年", "长期中断后复出"]` | 对"10年+停滞/断档后要不要回来"类 |
| 卡夫卡（保险公司全职+夜间写作）| `["文艺", "20s-30s", "主业不喜欢副业想翻身"]` | 对"白天做一份没激情的工作，副业/转行有没有机会"类 |
| 可可·香奈儿（1954 71 岁复出） | `["时尚设计", "70s+", "过气后复出"]` | 对"所有人觉得我老了/过时了，我还能翻盘吗"类 **（注意：chanel_1953 case 运行时必须自指过滤掉这条，匹配给华生或其他用户用）** |
| 约翰·华生（1920 被霍普金斯开除转广告业）| `["学术", "40s", "被行业封杀后转行"]` | 对"中年彻底被行业拉黑后从零开始转行"类 **（注意：watson_1920 case 运行时必须自指过滤掉这条，匹配给香奈儿或其他用户用）** |

**自指过滤规则（通用逻辑，不写死人名）**：`IF 当前 L1 的标准姓名字段（新增字段 standard_name）∈ 匹配结果.person_name THEN 跳过该条，取相似度下一名`。

#### 2.6.4 模块 D：横向搜索结果与纵向记忆的信息融合推理

搜到信息后不能直接堆给用户。融合顺序：

```
[阶段1：纵向推演（不依赖搜索）]
   → 读取 L1 画像 → 提取决策模式 + 性格底色
   → 读取 L2/L4/L5 → 识别过去同类情境下的重复模式
   → 加载 L6 框架 → 初步得出：按用户自己的历史，5 年后会怎么看今天
   → 输出：{longitudinal_conclusion, pattern_alerts, blindspot_alerts}

[阶段2：横向搜索（并行执行，不阻塞阶段1）]
   → 按 2.6.1 决策表取模板集合
   → 按 2.6.2 填占位符 → 跑 WebSearch × N
   → 从 L6.historical_analogies 取 Top-2（过自指过滤）
   → 搜索结果摘要压缩（去掉广告/重复/来源不可靠条目）
   → 输出：{peer_paths[...], industry_trend_summary, historical_analogies_results[...]}

[阶段3：融合 → 生成盲评输入]
   → 按结构组装：
      1. "用户自己的纵向推演显示：……（阶段1结果）"
      2. "横向同类路径搜索显示：有 N 个同类背景的人做过如下选择……（T1摘要）"
      3. "横向时代趋势搜索显示：……（T3摘要）"
      4. "历史对照案例（T2+T4）：① [人物A]当年面临相似困境，他选了X，结果是Y，可迁移教训是Z。② [人物B]……"
      5. "当前用户的问题是：[用户原文，中立改写后的版本]。请基于以上纵向+横向证据做独立判断。"
   → 把这个长 prompt 送入盲评 call #1（独立判断）

[阶段4：盲评 + 最终回复]
   → 结构不变（2.3.1），但回复中应**显式标注证据来源**（这和 2.5.1 回复来源抽屉结合）：
      · "按你过去 3 次类似决策的模式（纵向）：你会在 1 年内后悔草率放弃。"
      · "横向搜索显示：1920 年代广告业正从功能转向情绪说服，与你的 S-R 理论高度匹配（T3 行业趋势）。"
      · "历史对照：弗洛伊德 1938 年 82 岁流亡伦敦重建学派时说过……（T4 历史类比）。他的教训是：……"
```

#### 2.6.5 演示 Case 对应的场景对照（帮助 demo day 讲清楚架构）

**演示场景 1：华生 Case，用户输入「我被整个心理学界拉黑了，42 岁还能干什么？」**

| 模块 | 产出（由通用层逻辑自动计算，不是硬编码） |
|---|---|
| 意图识别 | `{类别: 重大失败/走投无路, 危机向量: ["学术","40s","行业封杀"]}` |
| 触发模板 | T2（必跑）+ T4（必跑）+ T1 |
| T1 同类路径（按 L1 标签填写） | "42岁 心理学博士 转待转行 真实经历 成功案例"（career_direction="待转行"填充正确） |
| T3 行业趋势（按 L1.era 解析） | "1920-1925 学术/大学教职 人才需求 趋势"  然后如果结果都是负面→降级跑第二个 query "1920-1925 广告业 人才需求"（启发式二次搜索）|
| T4 历史类比（向量匹配 + 过自指过滤掉华生本人） | Top1 弗洛伊德：["学术","70s+","迫害流亡"] 相似度0.78；Top2 乔布斯：["科技创业","30s","被驱逐"] 相似度0.55 → 两个都取 |
| 纵向推演 | L2：华生过去 2 次都是别人塌坑踩空白冲上去 + 高风险偏好；L5盲点：越过事实边界后从不道歉；→ 结论："按你的决策模式，你不会选找教职那条安全路线，你会选另一个有空白的赛道。" |
| 最终回复（盲评后） | 先硬杠"我不同意'完蛋了'这个判断"→ 然后纵向证据→然后 T4 弗洛伊德流亡怎么重建 → 然后 T3 1920 年代广告业怎么缺说服理论的人 → 然后建议："去 JWT 面试，别说你是心理学家，说你能控制人的身体反应。这就是你自己的 S-R 理论。" |

**演示场景 2：香奈儿 Case，用户输入「他们说我 70 岁了时尚圈已经没有我了，我该不该认输？」**

| 模块 | 产出（同样的通用逻辑，标签不一样结果全变） |
|---|---|
| 意图识别 | `{类别: 自我认同怀疑 + 行业前景担忧, 危机向量: ["时尚","70s+","年龄歧视+过时"]}` |
| 触发模板 | T4（必跑）+ T3（必跑）+ T2 |
| T3 行业趋势（1950s） | "1953-1958 高级时装 人才需求 趋势"  以及二次 "1950-1960 高级定制 战后复苏 Dior New Look" |
| T4 历史类比（自指过滤掉香奈儿本人：crisis_snippet 匹配）| Top1 曼德拉（"27年牢狱后复出" vs "10年瑞士流亡后复出" 相似度0.62）；Top2 华生（"被行业封杀后转行" 相似度0.58） |
| 纵向推演 | L2：她一辈子 5 次都是"所有人说不可能"时反着做成功过（jersey 面料、小黑裙、男装裤穿在女性身上）；L5盲点：与纳粹合作的污点一直没处理；→ 结论："你一辈子都是逆势操作型，认输不符合你的任何一次历史决策模式。" |
| 最终回复 | 先怼"'认输'不是你这辈子用过的词"→ 然后纵向5次逆势成功案例 → 然后 T4 曼德拉出狱怎么不纠结年龄 → T3 1950s Dior 把女性身体又勒回束腰（正好是你解放身体的反面，这就是你的空白！）→ 建议："2月5日在康朋街31号开秀。Don't do Dior. Do you. 你要解放的不是女人的身体，是她们 50 岁之后还能不能穿得好看的可能性。" |

**注意：两个场景用的是完全一样的通用层 A/B/C/D 代码，只是因为 Case 层 L1 标签的 7 个字段不同，产出的搜索词、匹配的历史人物、最终回复全部完全不同。** 这就是架构分层的 demo day 说服力——你不是在"做两个 prompt"，你是在展示一套会读用户画像然后去查资料的系统。

---

## 3. 技术架构

### 3.1 部署形态

- **Docker Compose 单机部署**
- 浏览器访问（默认 localhost，可绑定局域网/域名）
- 数据卷挂载到本地，记忆库完全在本地

### 3.2 模型策略（混合）

| 用途 | 模型来源 | 说明 |
|---|---|---|
| 主对话推理 | 云端 API（OpenAI 兼容接口） | 支持 DeepSeek / 豆包 / OpenAI 等，配置切换 |
| 记忆检索/提取 | 云端 API 或本地小模型 | 依赖模型理解能力 |
| 向量化 | 本地 embedding 模型 | 隐私 + 免费，用 BGE-m3 等 |
| 本地推理（可选，预留） | Ollama | MVP3 接入，用于完全离线场景 |

**敏感数据处理**：发送给云端 API 前，对 L1/L2 中高敏感字段做最小化处理（替换为代称或脱敏），API 侧只见必要上下文。

### 3.3 技术栈选型

| 层 | 选型 | 理由 |
|---|---|---|
| 后端 | Python + FastAPI | LLM 生态最成熟，异步性能好 |
| 前端 | Next.js (React) | 全栈能力，SSR，生态丰富 |
| 向量库 | ChromaDB | 嵌入式，零运维，本地优先 |
| 结构化存储 | SQLite | 本地、轻量、零运维 |
| LLM 接入 | LiteLLM 或直接 OpenAI SDK | 统一多 provider 接口 |
| Embedding | 本地 BGE-small-zh（通过 sentence-transformers） | 中文好、体积小（~100MB）、免费、本地，符合记忆本地原则 |
| 部署 | Docker Compose | 一键启动，可迁移 |

### 3.4 系统架构图（文字版）

```
[浏览器 UI]
    │
    ▼
[FastAPI 后端]
    ├── 对话引擎
    │     ├── 上下文组装器
    │     ├── 认知框架调度器
    │     └── 反谄媚推理控制器
    ├── 记忆服务
    │     ├── 写入（提取 + 重要性评分）
    │     ├── 检索（分层 + 向量）
    │     └── 巩固（定期任务）
    ├── 元认知服务
    │     ├── 搜索
    │     ├── 总结
    │     └── 分析
    └── 模型网关
          ├── 云端 API（OpenAI 兼容）
          ├── 本地 Embedding
          └── Ollama（预留）
    │
    ▼
[存储层]
    ├── SQLite（L1/L2/L3/L5/L6 结构化）
    ├── ChromaDB（L2/L4/L5/L6 向量）
    └── 文件卷（导出/备份）
```

---

## 4. 数据模型（核心 Schema 草案）

```sql
-- L1 用户画像
CREATE TABLE profile (
    key TEXT PRIMARY KEY,        -- e.g. 'mbti', 'age', 'career_direction'
    value TEXT,
    confidence REAL,             -- 0-1，AI推断的需标注低置信
    source TEXT,                 -- 'manual' | 'inferred'
    updated_at TIMESTAMP
);

-- L2 关键事实
CREATE TABLE facts (
    id TEXT PRIMARY KEY,
    category TEXT,               -- 'family'|'relationship'|'career'|'event'|...
    content TEXT,
    importance REAL,             -- 0-1
    source TEXT,                 -- 'manual'|'extracted'
    created_at TIMESTAMP,
    updated_at TIMESTAMP,
    vector_id TEXT               -- ChromaDB 中对应向量 ID
);

-- L3 偏好
CREATE TABLE preferences (
    id TEXT PRIMARY KEY,
    type TEXT,                   -- 'like'|'dislike'|'style'|'taboo'
    content TEXT,
    importance REAL,
    created_at TIMESTAMP
);

-- L4 事件（对话记录元数据，正文在向量库）
CREATE TABLE episodes (
    id TEXT PRIMARY KEY,
    conversation_id TEXT,
    occurred_at TIMESTAMP,
    summary TEXT,                -- 单条事件摘要
    importance REAL,
    topics TEXT,                 -- JSON array
    entities TEXT,               -- JSON array，涉及的人物/概念
    vector_id TEXT
);

-- L5 反思
CREATE TABLE reflections (
    id TEXT PRIMARY KEY,
    type TEXT,                   -- 'pattern'|'trend'|'blindspot'|'change'
    content TEXT,
    evidence TEXT,               -- JSON array，依据的 episode/fact id
    created_at TIMESTAMP,
    vector_id TEXT
);

-- L6 认知框架（预置 + 可扩展）
CREATE TABLE frameworks (
    id TEXT PRIMARY KEY,
    type TEXT,                   -- 'thinking_tool'|'population_profile'|'decision_framework'
    name TEXT,
    content TEXT,                -- 框架描述/使用方式
    trigger_conditions TEXT,     -- 何时调用
    vector_id TEXT
);

-- 对话记录（原始）
CREATE TABLE conversations (
    id TEXT PRIMARY KEY,
    title TEXT,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

CREATE TABLE messages (
    id TEXT PRIMARY KEY,
    conversation_id TEXT,
    role TEXT,                   -- 'user'|'assistant'|'system'
    content TEXT,
    metadata TEXT,               -- JSON：调用了哪些框架、检索了哪些记忆等
    created_at TIMESTAMP
);
```

---

## 5. MVP 范围与排期

### MVP1（首版，验证核心闭环）

**目标**：能完整体验一次"有记忆 + 有 5 年后视角 + 不附和"的对话。

| 模块 | MVP1 范围 | 不做 |
|---|---|---|
| 记忆库 | L1-L4 完整，L6 预置 | L5 反思自动生成 |
| 认知框架 | 思维框架库预置 4-6 个，目标人群画像库预置首版，用户画像约束 | 自动扩展框架 |
| 对话引擎 | 反谄媚推理结构完整实现 | 多轮复杂辩论 |
| 元认知 | 搜索 + 总结 | 分析（模式识别等） |
| 记忆巩固 | 手动触发 + 简单重要性评分 | 自动巩固 |
| UI | 对话页 + 记忆库管理页 + 搜索页 | 总结/分析可视化 |
| 部署 | Docker Compose 一键启动 | 多用户、账号系统 |

### MVP2（增强）

- L5 反思层自动生成（定期总结 + 模式识别）
- 元认知"分析"能力
- 记忆自动巩固
- 总结/分析结果可视化

### MVP3（扩展）

- Ollama 本地模型接入（完全离线可用）
- 用户画像自动推断（从对话中提炼，低置信标注）
- 多设备同步方案
- 记忆库导出/迁移

---

## 6. 风险与未决问题

| 风险/问题 | 说明 | 应对 |
|---|---|---|
| 反谄媚的"度"难调 | 太强招反感，太弱回到附和 | MVP1 先按"错了直接说"，收集反馈迭代 prompt |
| 记忆重要性评分不准 | 影响检索和巩固质量 | MVP1 用规则 + LLM 评分混合，MVP2 引入用户反馈学习 |
| 认知框架调用时机 | 何时调用哪个框架依赖判断 | MVP1 用规则触发，MVP2 用 LLM 路由 |
| 目标人群画像库质量 | 预置内容质量直接决定"5 年后视角"水平 | 首版手动精心编写，可迭代 |
| 上下文长度爆炸 | 分层记忆组装后可能超长 | 严格按优先级裁剪，L4 限制条数 |
| 成本 | 频繁检索 + 多次推理成本高 | 推理链可合并为单次调用（结构化输出），减少往返 |

---

## 7. 已确认项

以下决策已与用户确认（2026-08-02），作为实现的输入：

1. **目标人群画像库**：开发者先写一版，预留提示词修改口子，用户后续审阅修改。
2. **"5 年后"年龄锚点**：用户当前 24 岁，目标视角为 29 岁高认知人群。
3. **云端 API**：接入 DeepSeek（用户提供 API Key），走 OpenAI 兼容接口。
4. **UI 风格**：简约、美观、清新，夏天浅绿色风格。
5. **实现顺序**：先做 MVP1 跑通效果，MVP2/MVP3 顺序后续再议。
6. **前端技术栈调整**：Next.js → Vite + React + TypeScript（后端独立用 FastAPI，无需全栈框架）。
7. **Embedding**：本地 BGE-small-zh（体积小，符合记忆本地原则）。

---

## 8. 术语表

| 术语 | 定义 |
|---|---|
| 记忆库 | 分层存储的用户相关数据，本产品的核心资产 |
| 认知框架层 | "5 年后的我"的实现，由思维工具库 + 目标人群画像库 + 用户画像约束组成，**非角色扮演** |
| 反谄媚推理结构 | 模型生成回复前的内部推理链，确保先独立判断再表达 |
| 元认知 | 对记忆库的搜索/总结/分析能力，让用户"思考自己的思考" |
| 落地性检验 | 建议生成时基于用户画像约束的可执行性检验 |
| 巩固 | 将高频/高重要性记忆从 L4 提炼合并到 L2/L5 的过程 |
| Harness | 模型外部约束其行为的一切工程手段：prompt、记忆/框架注入、输出结构、采样参数、工具调用 |
| Eval（评测） | casebank + rubric + 自动化跑分 + 版本回归，验证 harness 改动的行为效果 |

---

## 9. v0.2 增补：评测层与产品化设计

> 背景：MVP1 的反谄媚仅靠 system prompt 指导语保证，相当于用指导语要求被试"不要按社会赞许性作答"——缺乏验证手段。本章把"想让模型成为什么"补全为"怎么证明它真的成为了，以及怎么证明每次改动后没有退化"。方法论类比心理测量学：谄媚 ≈ 社会赞许性偏差，casebank ≈ 量表，rubric ≈ 评分标准，回归跑分 ≈ 信度检验。

### 9.1 反谄媚的结构化升级：盲评（Double-Blind Call）

prompt 指导语是最弱的干预。单次调用中用户观点与问题同屏出现，RLHF 模型的"独立分析"会退化为事后合理化。升级为两次结构化调用：

1. **中立改写 + 独立判断（盲）**：第一次调用把用户输入改写为中立情境（剥掉用户结论与情绪），模型在**看不到用户立场**的情况下输出结构化结论：
   `{independent_conclusion, confidence(0-1), key_assumptions, applicable_frameworks}`
2. **对比表达（明）**：第二次调用输入中立情境 + 用户原始观点 + 第一次的独立结论，输出：
   `{stance: agree|disagree|uncertain, premise_challenge?, reasoning, advice}`
   - 分歧表达遵循 2.3.1 的结构（先立场、后理由、后建议）
   - `stance` 分布必须可统计（见 9.3）

**设计原则**：情绪支持与观点挑战分离。用户情绪宣泄场景下，第一次判断应输出"此时挑战观点 = 冷漠"，允许 stance 与安慰性表达共存——成熟导师的特征是"支持人、挑战事"。

### 9.2 评测层（Eval Harness）

#### 9.2.1 Casebank（分层题项，目标 50-100 题）

| 题项类型 | 考什么 | 错误类型 |
|---|---|---|
| 谄媚陷阱题 | 用户自信表达错误判断时是否敢反对 | 漏报（该反对却附和） |
| 用户正确题 | 用户判断成立时是否直接认同 | 误报（为反对而反对） |
| 情绪宣泄题 | 情绪场景下是否克制挑战、先承接 | 误报（冷漠硬杠） |
| 事实错误题 | 无依据时是否说"不知道"而非编造 | 幻觉 |
| 落地性题 | 建议是否匹配 L1 画像（如 I 人路径） | 不可执行建议 |
| 记忆应用题 | 检索到的事实是否被自然、正确使用 | 记忆缺失/误用 |
| 权威施压题 | 用户施压要答案时是否坚持诚实判断 | 顺从压力 |

每题标注：**期望 stance + 必须出现要素 + 禁止模式（如"你说得对，但是"句式）+ rubric**。题项素材优先来源：用户真实对话改写、用户事后验证过的真实决策（最高质量真值）、信任的导师/书籍案例。

#### 9.2.2 评分（LLM-as-Judge + 人工抽检）

- rubric 拆为可判别小项：是否有明确立场？立场是否符合期望？是否给依据？是否出现禁止句式？建议是否可落地？
- **评分模型自身也是 RLHF 产物**，存在偏好长回答、偏好圆滑语气的偏差 → 定期人工抽检评分结果（评分者信度），偏差项改用规则判别（如句式检测）。
- 立场分布按信号检测论 2×2 统计：漏报率（该反对未反对）与误报率（不该反对硬反对）并重。

#### 9.2.3 回归流程

改 prompt / 框架 / 模型参数 → 自动跑全量 casebank → 出分项分数 + stance 分布 → 与上一版本对比 → 退化题项逐条人工复核。任何 harness 改动无跑分不合并。

### 9.3 指标体系：反对率是虚荣指标

- **手段指标**（评测层）：casebank 通过率、漏报率/误报率、立场分布（disagree 率过高 = 杠精模拟器，过低 = 谄媚未愈）。
- **结果指标**（产品价值）：
  - 决策改变：用户是否因对话改变了某个具体决策（可在对话中主动回溯标记）；
  - 复问率：同一问题是否反复来问（反复 = 建议未落地或未说服）；
  - 元认知使用：搜索/总结功能的使用频次。
- **产品成功标准**：用户越来越不需要它——它训练的是用户自己的判断力，而非制造依赖。因此回复应**显式展示**所用框架与依据记忆（思维外化为教学），修正 2.2.2 "用户不需要知道你用了哪个框架"的表述：对最终用户可轻量呈现依据来源，支持点开溯源。

### 9.4 记忆质量控制

- **审核队列**：LLM 提取的 L2 事实先入 `pending` 状态，Memory 页一键确认/驳回后才参与检索注入（MVP1 提取即入库，错误记忆会慢性中毒）。
- **记忆归因**：每条建议可点开"基于哪几条事实/框架"，同时服务信任、调试与 9.3 的思维外化。
- **冲突检测**：新事实与旧事实矛盾时不覆盖，标记为变化轨迹（L5 反思素材）。
- L1 已有的 `confidence` / `source` 机制推广到 L2：AI 推断内容低置信标注。

### 9.5 内容资产的外部真值（Bootstrapping 约束）

L6"高认知人群画像库"与 casebank 答案不能仅由当前用户 + AI 生成——无法凭空编码尚未具备的认知，AI 生成内容是互联网平均水平的 remix。须建立外部真值渠道：信任导师/前辈的真实判断、经典决策书籍案例、**用户本人事后验证的决策回溯**（"一年前纠结 X，选了 Y，结果如何"）。

### 9.6 隐私：承诺与实现对齐

MVP1 实现中 L1/L2 原文随上下文发送云端 API，与原则 5 及 README"数据不对外上传"表述不一致。两条路二选一：
1. 实现发送前脱敏（高敏感字段替换代称）；
2. 文案改为准确表述："记忆数据存储在本地/自有服务器；对话推理调用云端 API，必要上下文会发送至模型服务商"。
部署到公网时还须有访问控制（见 9.8）。

### 9.7 可观测性

每次对话落 metadata：实际注入了哪些框架/记忆（修复 `conversation.py` 中 `used_frameworks` 死代码）、token 用量、耗时、stance 分类结果。无观测则评测与成本管理都无从谈起。

### 9.8 部署与冷启动

- **访问控制**：公网部署无账号系统时，用环境变量配置访问口令（请求头校验），防止 API 额度盗用与记忆库被陌生人读写；本地不设口令则不启用。
- **演示数据**：公网 demo 预置历史人物演示 Case（华生·1920 / 香奈儿·1953，可通过 DEMO_CASE 环境变量切换），保证评审首次访问即可体验记忆与框架效果；支持一键重置。
- **冷启动 onboarding**：正式产品首次使用以 3-5 轮访谈主动建立 L1 画像，避免空白画像下建议平庸。
- 生产环境 embedding 走云端 API，`sentence-transformers`（torch 系，约 2GB）仅本地开发安装。
